---
title: Library Durable Change Log
type: meta
status: active
visibility: public
created: 2026-08-23
updated: 2026-08-30
sources: [docs/SESSION_HANDOFF.md]
tags: [library, changelog]
---

# Durable Change Log

## 2026-08-23

- Established Hermes profile `library` with dedicated agent identity **Shelfkeeper**.
- Kept persistent memory disabled and cron empty.
- Added selected project-only skills and DM-only Telegram routing placeholders.
- Established The Knowledge Shelf with 11 reading guides and 44 audio entries.
- Added original public-post artwork covers and local audio thumbnails.
- Added responsive one-shelf-per-row layout and approval-gated public publishing.
- Added public and private Git remotes plus a dedicated Google Drive project folder.
- Initialized minimal agent-readable wiki, decision records, and runbooks.
- Added a public-safe dedicated agent profile blueprint with a portable master prompt for other AI systems.
- Rendered the blueprint as a 19-view interactive single-file HTML manual, verified desktop/mobile behavior, and stored the review copy in the designated private workspace without adding it to the public shelf.
- Published the blueprint as the twelfth reading guide, recorded its scoped custom-cover exception under `assets/covers/custom/`, and verified the live shelf, guide, and cover against local hashes.
- Replaced all 12 reading-book shelf titles with user-approved Thai-first labels and verified that none overflow or overlap publication dates on desktop or mobile.
- Redesigned all 44 audio cards as iPod-style players, removed the `AUDIO BOOK` kicker, moved duration into the lower device panel above PLAY, and moved reading categories to responsive metal plaques on shelf edges.
- Removed the redundant reading-category filter row; category search still works through the main search field and responsive shelf plaques remain the visible category labels.
- Replaced active Facebook artwork references with 12 individually designed custom reading covers, preserved reproducible HTML/logo sources under `templates/`, and retained legacy Facebook assets without catalog references.
- Imported `hermes-memory` and `hermes-guardian` with Git subtree history, audited both against the existing collection, confirmed distinct content, and added two custom-covered reading entries.

## 2026-08-24

- Added `Hermes_Trustworthy_Autonomy_Manual.html` unchanged as the fifteenth reading guide, designed an industrial safety-standard custom cover, and catalogued it as **Hermes ทำงานเองอย่างไว้ใจได้**.
- Added the 45th audio entry **ยิ่งเพิ่มสกิล AI ยิ่งโกหกเก่งขึ้น** from playlist Video ID `wQ_XZKFgmOk`, using user-verified duration/date and a cached proxy-fetched thumbnail.
- Made reading shelf category plaques clickable filters with active metal styling and click-again reset behavior.
- Added `AI_Chatbot_Beyond_ChatGPT_Interactive_Reference_Manual.html` unchanged as the sixteenth reading guide, designed a layered product-stack custom cover with an official ChatGPT icon, and catalogued it as **สร้าง AI Chatbot ให้เหนือกว่า ChatGPT**.
- Sanitized and reorganized a private-system backup prompt into the public-safe **สำรองและกู้คืน Hermes Profile** manual, created 13 interactive views plus a detailed Markdown source, designed a disaster-recovery cover, and prepared it as the seventeenth reading guide.
- Replaced the backup guide's original operator illustration with the approved monochrome Hermes operator logo and retuned the cover to black, warm ivory, safety red, and muted gold.
- Produced and published the YouTube podcast **คู่มือสำรองและกู้คืนโปรไฟล์ Hermes อย่างปลอดภัย ฉบับสมบูรณ์** with a sound-reactive red equalizer, verified custom thumbnail, and public Video ID `aGSn1viukAM`; added it as the 46th Audio Shelf entry.

## 2026-08-25

- Rewrote the public transcript **Every Hermes Agent Concept Explained for Normal People** as the public-safe Thai guide **เข้าใจ Hermes Agent สำหรับคนทั่วไป**, removed promotions and affiliate links, corrected key claims against current Hermes docs/CLI, built a 10-view interactive manual, and designed a user-approved monochrome system-orbit cover with red reserved for the `Hermes Agent` title.
- Produced and published the YouTube podcast **ในหัวของ Hermes มีอะไร? แกะระบบ AI Agent ให้คนทั่วไปเข้าใจ** with a transparent sound-reactive Cyan/Coral equalizer, verified Full HD/custom thumbnail, and public Video ID `KtHYNnLM_Dk`; verified it at position 1 in playlist `tech (Ai)` and added it as the 47th Audio Shelf entry.
- Replaced that podcast with the corrected Public video **ในหัวของ Hermes มีอะไร? แกะระบบ AI Agent ให้คนทั่วไปเข้าใจ | ฉบับแก้ไข** (`4zmH_6gM4h4`), removed the 41.099-second Dead Air interval, rebuilt the lower artwork as a clean Cyan-to-Coral audio-reactive visualization deck, moved the corrected video to playlist position 1, changed the original video to Unlisted, and updated the existing Audio Shelf entry without increasing the 47-item count.
- Catalogued the user-uploaded **Agent Reach vs Direct Transcript — เปรียบเทียบจากการทดสอบจริง** comparison page unchanged as the nineteenth reading guide and added a split Cyan/Coral 600×900 custom cover with reproducible HTML source.
- Rewrote **Everything Goldman Sachs Taught Me About AI** as the public-safe Thai guide **VAULT: คู่มือคุม AI ไม่ให้พลาด**, clarified that VAULT is Nate Herk's mnemonic rather than an official Goldman Sachs framework, integrated Goldman/NIST/Microsoft fact-check sources, built an 11-view offline Interactive HTML manual, and designed the selected Cobalt Ice vault cover.
- Published the companion YouTube podcast **คุม AI ไม่ให้พลาด: กรอบ VAULT สำหรับระบบที่ตรวจสอบได้และควบคุมความเสี่ยง** (`lL2eb4GeoAU`) with the approved Compact Vault Bars, verified Public 1080p/custom thumbnail and playlist position 1 in `tech (Ai)`, and added it as the 48th Audio Shelf entry.
- Added the third generated collection **App Shelf** with three source-provenance catalog entries from `p2544/battle-tank`, `p2544/bakery-center`, and `p2544/loderunner`; preserved Battle Tank byte-for-byte, hardened Bakery Center against malicious JSON/AI-import Stored XSS, imported only the required Lode Runner runtime behind a restricted stable wrapper, and designed each shelf item as a content-specific 3.5-inch diskette label in a light Pantip Plaza 1990s software-floor theme.
- Renamed the App Shelf storefront header to **พันธุ์ทิพย์พลาซ่า** and replaced the static software sign with a slow continuous left-to-right LED shop-message marquee that changes red, green, yellow, and blue on successive loops.
- Redesigned the Audio Shelf environment as a light premium technology gallery inspired by Apple Fifth Avenue's glass cube, skylights, pale stone, off-white terrazzo, stainless steel, maple product tables, natural light, restrained system typography, and single blue interaction accent while preserving all 48 iPod-style players and responsive shelf behavior.
- Renamed the Audio collection header to **The Audio Shelf**, replaced the App header's `3½` badge with the user-supplied Pantip Plaza logo, and reversed the Pantip LED marquee to travel continuously from right to left while retaining its four-color loop.
- Added four playable App entries: preserved the GPL-3.0 Pac-Man HTML5 runtime, created a Galaga browser port from pinned Unity sprites/configs, created an RL Battle City manual/AI-autopilot browser port from the MIT Python/Gym source, and created a New Rally-X browser port from pinned MonoGame assets. Expanded the App Shelf to seven diskettes, added source-derived WebP cover stickers to every disk except Bakery Center, and replaced the former building icon/favicon with the user-supplied Shelfkeeper Librarian image.
- Withdrew **Galaga**, **RL Battle City**, and **New Rally-X** from the active App Shelf at the user's request; removed their catalog records, stable launcher URLs, runtime directories, source-derived stickers, and active audit entries. Pac-Man, Bakery Center, Battle Tank, and Lode Runner remain active.
- Redesigned the Reading collection as **Coffee and Books** using a bright daylight garden-café photograph, heavy translucent iOS-inspired glass for navigation/header/search/bookshelf surfaces, a leaf latte-art image in the title panel, transparent glass shelf planks, and text-only metadata above every cover. Added public source attribution for the local visual assets.
- Replaced the separate Latte panel and CSS blend with one approved native-resolution retouched header asset. Deterministic two-scale inpainting removes baked left-side UI without touching the cup/table photographic zone; Production overlays the logo and **Coffee and Books** title as semantic DOM and retains the text-only book metadata rule.
- Aligned the Coffee and Books Navigation, Title, Reading/Search, and Bookshelf panels to one shared responsive width, restored the garden photograph's natural color by removing brightness/saturation/contrast and color-wash overlays, and changed the retouched header image to `cover` so no blank cream side strip remains.
- Replaced the original indoor café wallpaper—which contained a pale ceiling/roller-shade rectangle in its upper-right—with a new outdoor garden photograph by Ran Ding showing only lush trees, foliage, grass, and natural midday sunlight. Cached it locally as an EXIF-free 1800×1200 WebP under the Unsplash License.
- Fixed a scope leak where Coffee and Books rules had been appended to shared `library.css` and unintentionally restyled the App collection. Restored the shared stylesheet byte-for-byte from `ca74827`, moved all Coffee rules into `reading-library.css`, and loaded that file only from the Reading template. App screenshots now match the backup everywhere except the expected moving LED frame; Audio retains its Apple-inspired design.
- Fixed the Pantip LED appearing frozen when the browser/OS enables Reduce Motion. The broad shared accessibility rule had reduced the 28-second marquee to one 0.01ms iteration; a later App-scoped important override now keeps this functional storefront sign moving right-to-left for 28 seconds infinitely while all other decorative animations continue respecting the shared preference.
- Applied the approved Audio wallpaper Option 3: the Apple Fifth Avenue retail floor by Seasider53 (CC BY 4.0). Added the local 1600×1200 EXIF-free WebP and attribution report, then converted Audio navigation/header/search/bookshelf surfaces and shelf planks to translucent iOS-style Liquid Glass while preserving all 48 iPods and responsive shelf reconstruction.
- Shortened the initial Audio Shelf with progressive disclosure: show the newest 10, load 10 more, show all 48, or collapse to 10. Added a lightweight year archive generated from existing publication timestamps; Search bypasses the limit to query all 48 and resets to the newest 10 when cleared. Server HTML remains complete for progressive enhancement and No-JS access.
- Added a shared Sticky Floating Library Dock loaded after each collection's own theme stylesheet. The centered three-segment Liquid Glass switcher remains visible while scrolling, keeps icon + label text on desktop and mobile, and highlights Reading green, Audio Apple Blue, or App Pantip Cyan without altering collection-specific page styling.

## 2026-08-26

- Added the fourth generated collection, **Gallery**, from `data/gallery.json` with eight original project infographic PNGs, a user/project-owner supplied EXIF-free WebP hero, filter/sort, grid/list modes, and a keyboard-accessible lightbox with focus restoration. Expanded the shared Sticky Dock to four equal segments and documented the Gallery generator and operating runbook; changes remain local pending separate publication approval.
- Added two user/project-owner supplied works at the front of Gallery: **Hermes x Home Assistant** (A4 portrait, Development) and **ทำไม AI Agent ถึงเข้าเว็บนี้ไม่ได้?** (16:9 landscape, Security). Converted both JPEG sources to EXIF-free RGB WebP assets, expanded the catalog to 10 records, and retained generated-page ownership and responsive/lightbox behavior.
- Added Gallery lightbox zoom and pan: −/+/100% controls, 100–400% bounds, keyboard zoom shortcuts, and clamped pointer dragging across mouse/touch/pen. Image changes and close actions reset zoom/pan to the default viewport.
- Fixed incomplete Gallery panning at high zoom. The image and caption had shared one implicit CSS grid, shifting the real image center away from the clamp calculation. A dedicated media viewport now centers and clips only the image; measured edge tests confirm left/right/top/bottom coverage within 1px on desktop and touch-driven left/right coverage within 1px on mobile.
- Added the user-supplied **PDF Password Remover** to the App Shelf as a byte-preserved single-file utility. Extended App provenance policy for owner-supplied files without inventing Git history, verified `qpdf-run@0.2.1` MIT metadata and all three CDN SHA-384 pins, and completed an encrypted-PDF browser smoke test whose downloaded output was unencrypted with text preserved.
- Added the byte-preserved interactive guide **Claude Prompt Caching** to the Reading collection with its original date, 21 June 2026. Created a 600×900 EXIF-free editorial cover using the Claude Code Spark as a secondary, officially documented brand cue, a stable-prefix/cache-hit metaphor, and a visible Anthropic non-affiliation notice.
- Removed the eight Specialist-generated example artworks and their public PNG assets from Gallery at the user's request. Gallery now contains only the two user-supplied WebP works; private prototype sources remain local and untracked.
- Published and production-verified the approved reading guide **Grok Bot vs Claude Code vs Codex** from the supplied research: 11 interactive views, 13 official references, three embedded transparent brand assets, and a 600×900 minimalist editorial cover contrasting a persistent teammate with spawned workers.
- Published YouTube podcast `VQdCzVNhTmI` on channel **manny calavara** using the approved Tricolor Segment Bars render, Public visibility, Not made for kids, custom 1280×720 thumbnail, 13-source description, clean copyright checks, and verified membership in the exact `tech (Ai)` playlist. Added it as Audio Shelf position 1 with a local 480×360 cover.

## 2026-08-27

- Added the user-supplied **Hermes Agent v0.20 (Herald Release)** infographic to Gallery with the requested publication date **4 August 2026**. Converted the JPEG to an EXIF-free 905×1280 RGB WebP, catalogued it as Development/A4, regenerated `gallery.html`, and verified the three-artwork layout on desktop and 390px mobile; changes remain local pending publication approval.
- Added the user-supplied **Grok Bot: จุดที่ต้องระวังและข้อจำกัด** infographic to Gallery as Security/4:5, using the local-addition date **27 August 2026** because no separate publication date was supplied. Converted the JPEG to an EXIF-free 1024×1280 RGB WebP, regenerated the four-artwork page, and verified desktop and 390px mobile layouts. Recorded the interpretation rule that “เอารูปเข้าแกลเลอรี” means perform the catalog operation rather than reply with an image description.
- Published both pending Gallery additions in commit `6844829`: public and private remote HEADs matched, GitHub Pages deployment `33055457583` succeeded, Production rendered all four cards with both requested dates, the Grok artwork hash matched Local, and desktop/mobile DOM verification found no horizontal overflow or broken catalog images.
- Added the user-supplied **Grok Bot: จุดที่ควรระวัง** infographic to Gallery as Security/4:5 using the local-addition date **27 August 2026**. Converted the JPEG to an EXIF-free 1024×1280 RGB WebP, regenerated the five-artwork page, and verified desktop and 390px mobile layouts.
- Adopted the project-owner-approved automatic publication exception for original Gallery image uploads: after full quality gates, Shelfkeeper may catalog, commit, push to both Library remotes, and publish without a second confirmation while preserving visible image content. Reading, Audio, App, removals, and all unrelated public changes remain approval-gated.
- Published **Grok Bot: จุดที่ควรระวัง** and the Gallery-only standing approval policy in commit `129a981`; both remote HEADs matched, GitHub Pages deployment `33056751785` succeeded, Production rendered five cards with the correct title/date and no mobile horizontal overflow, and the Production artwork hash matched Local exactly.
- Added **คู่มือ Hermes Agent Advance Computer Use** as the newest of 23 Reading entries dated 27 August 2026. The final user-supplied HTML is preserved byte-for-byte as a 13-panel Practical Guide with Sidebar/Topbar navigation, search, tabs, theme/font state, copy controls, mobile navigation, checklists, print mode, and sources; its SHA-256 is locked by test. Replaced the preliminary cover with the final owner-supplied artwork, normalized only to an EXIF-free 600×900 RGB WebP. Desktop/mobile visuals, interactive CDP checks, official link checks, and full project gates passed; changes remain local pending Reading publication approval.
- Published **คู่มือ Hermes Agent Advance Computer Use** in commit `4b6f480`; public and private remote HEADs matched, GitHub Pages deployment `33061438459` succeeded, Production Reading contained 23 books with the new guide first, and live HTML/cover hashes matched Local exactly. Production mobile DOM confirmed 13 panels, 13 navigation items, Overview active, and no horizontal overflow.
- Prepared a byte-preserved replacement for **คู่มือ Hermes Agent Advance Computer Use** using the owner's updated HTML (`1ff59d8…`), which embeds the Hermes Agent logo and refines sidebar branding while retaining the stable public URL, 13-panel navigation, catalog metadata, date, cover, and Reading position. Regression, JavaScript, interaction, desktop/mobile visual, and privacy checks passed locally; the published version remains unchanged pending explicit Reading push approval.
- Published the updated **คู่มือ Hermes Agent Advance Computer Use** branding in commit `52cd987`; public/private remote HEADs matched, GitHub Pages deployment `33062292220` succeeded, Production SHA-256 matched `1ff59d8…`, and live mobile DOM confirmed the embedded Hermes Agent logo, 13 panels, 13 navigation items, Overview active, and no horizontal overflow.
- Published the YouTube podcast **Agent Reach: มอบดวงตาให้ AI ทะลวง Social Platform** (`UtzEjC9G3Dg`) with the approved Target Water Ripple visualization. Verified Public 1080p30, custom thumbnail, Not made for kids, clean copyright checks, exact `tech (Ai)` playlist membership at position 1, and public metadata read-back; added it as the 50th Audio Shelf entry.
- Added the owner-supplied **Agent Reach คู่มือฉบับไทย** as the 24th Reading entry. Preserved the 170,001-byte HTML byte-for-byte at SHA-256 `e3c92f5e…`, normalized the supplied portrait artwork only to an EXIF-free 600×900 RGB WebP, and verified 10 responsive sections/navigation items plus desktop/mobile behavior.
- Added **ตุ่มเงิน** from `p2544/tumngern` commit `fa43333` as the sixth App entry with the owner-requested date 30 July 2026. Imported the runtime under `app/tumngern/` as a path-adjusted derivative, retained source provenance, fixed all PWA/splash base paths, and verified splash/onboarding at 1440px and 390px with all assets returning 200 and no runtime exceptions.
- Published the Agent Reach Reading/Audio additions and **ตุ่มเงิน** App in commit `9ad661f`; public/private remote HEADs matched, GitHub Pages deployment `33070600654` succeeded, Production rendered Reading 24 / Audio 50 / App 6, all nine sampled Production artifact hashes matched Local, and live Tumngern mobile runtime loaded every PWA resource with HTTP 200 and no exceptions or overflow.

## 2026-08-28

- Added the owner-supplied **Personal Infrastructure Wiki** infographic to Gallery as Infrastructure/16:9 using the local-addition date **28 August 2026**. Converted the 1280×720 JPEG through its embedded color profile to a lossless sRGB WebP without cropping or visible edits, removed EXIF/ICC metadata, added the Infrastructure filter, and verified the six-artwork Gallery on desktop and 390px mobile.
- Published **Personal Infrastructure Wiki** in commit `6b5f5f6`; public/private remote HEADs matched, GitHub Pages deployment `33166761238` succeeded, Production rendered six Gallery cards with the correct 28/08/2026 date and Infrastructure filter, the Production image SHA-256 matched Local (`56e46ad1…`), and desktop/mobile lightbox checks found no runtime errors or horizontal overflow.
- Added two owner-supplied AI infographics to Gallery dated **28 August 2026**: **Skills vs Plugin vs Connector ใน Claude** and **ระบบ Memory, Project และ Computer ของ Grok Bot**. Converted both ICC-tagged JPEGs to lossless sRGB WebP without cropping or visible edits, removed EXIF/ICC metadata, expanded Gallery to eight works, and verified both images through the AI filter and desktop/mobile lightboxes.
- Published both AI infographics in commit `99fab2b`; public/private remote HEADs matched, GitHub Pages deployment `33172361550` succeeded, Production rendered eight Gallery cards with the AI filter and correct dates/dimensions, both Production image hashes matched Local (`8691b61c…`, `4cc3f89a…`), and desktop/mobile lightbox checks found no runtime errors or horizontal overflow.

## 2026-08-29

- Published the YouTube podcast **Alex Finn: ใช้ Grok Bot คุมกองทัพ AI ให้บริหารงานแทนเรา** (`oe3zUOLrLF4`) on channel **manny calavara** using the approved no-panel Edge Ladder Spectrum. The 12:33 video routes female speech to a Magenta/Violet top-left equalizer and male speech to an Electric Blue/Cyan bottom-right equalizer, with Public visibility, Not made for kids, custom thumbnail, clean copyright checks, verified 1080p30 public playback, and exact `tech (Ai)` playlist membership at position 1.
- Added `oe3zUOLrLF4` locally as Audio Shelf position 1 with a verified 480×360 cover, shifting the prior 50 entries to positions 2–51. Generated `audio-library.html` now contains 51 cards; focused tests and desktop/mobile first-card, search, link, date, duration, cover, and overflow checks passed. Public Library deployment remains pending the approved scoped push.
- Published the Audio Shelf addition in commit `4275d43`; local/public/private remote HEADs matched, GitHub Pages run `33204031199` succeeded, and Production returned 51 cards with the Alex Finn entry first/newest. The live 480×360 cover SHA-256 matched Local (`bb253041…`); desktop/mobile browser checks found no runtime errors, broken images, or horizontal overflow, and live search returned exactly the new item.
- Published the YouTube podcast **Grok AI ทางลัดสำหรับมือใหม่: ต่างจาก Claude, ChatGPT และ DeepSeek อย่างไร?** (`YivD8OO85TM`) on channel **manny calavara** using the approved Glass Spectrum Equalizer. The 18:47 Public video is Not made for kids, uses the owner-supplied custom thumbnail, passed clean copyright checks, includes five verified source URLs in its description, and is verified at position 1 in the exact Public `tech (Ai)` playlist.
- Added `YivD8OO85TM` locally as Audio Shelf position 1 with a verified 480×360 cover, shifting the prior 51 entries to positions 2–52. Generated `audio-library.html` now contains 52 cards; 87 tests, all four build checks, desktop/mobile first-card geometry, full-catalog search/reset, image integrity, and zero-overflow checks passed. Public Library deployment remains pending the approved scoped push.
- Published the Audio Shelf addition in commit `091b72f`; local/public/private remote HEADs matched and GitHub Pages run `33207676100` succeeded. Production returned 52 cards with `YivD8OO85TM` first/newest, and the live cover SHA-256 matched Local (`caec7d33…`). Desktop/mobile Production checks confirmed initial 10-item disclosure, full-catalog search returning exactly one new item, reset to 10, correct date/duration/link/cover, and zero horizontal overflow.

## 2026-08-30

- Published **Gemini Live API — คู่มือสร้าง Voice Agent แบบ Real-Time** in commit `6983d66`; public and private remote HEADs matched, Production returned 25 Reading cards with the Gemini entry first/newest, and live HTML/index/cover hashes matched Local. After owner feedback, rebuilt and published the guide in commit `8e659b4` to follow the latest successful Reading-book pattern from `interactive-reference-manuals` / owner-supplied artifacts: a sidebar/topbar Interactive Reference Manual with 10 views, hash navigation, search, theme/font controls, copy button, print mode, and reader-facing Thai without conversational particles. A follow-up owner correction removed the cover image from the Overview content while keeping the shelf cover asset/catalog entry intact.
- Recorded the owner-confirmed **skill fidelity and specialist orchestration** rule: every Library workflow should reuse the original recorded skill/runbook/history pattern by default; any format/process adaptation requires owner approval unless explicitly requested, and non-trivial multi-part work should use Shelfkeeper as orchestrator with worker/reviewer specialist agents following prior patterns.
- Added **คู่มือย้าย Hermes Profile ไปยัง Linux Server เครื่องใหม่** as the 26th Reading entry from the owner-supplied Markdown source. Built it as a 10-view `interactive-reference-manuals` HTML app with sidebar/topbar navigation, hash routing, search, theme/font controls, copy buttons, print mode, and the owner-requested correction that the large title appears only in the first Overview view rather than on every menu. Used the owner-supplied cover only as the shelf/catalog cover, normalized to EXIF-free 600×900 RGB WebP. Local verification passed 93 tests and all generated page drift checks; publication was explicitly approved in the current turn.
- Added **Visual Art Director Agent — Interactive Reference Manual** as the 27th Reading entry from the owner-supplied HTML and cover. Preserved the HTML byte-for-byte, normalized the owner cover only to EXIF-free 600×900 RGB WebP for the shelf, catalogued it first/newest under `AI Design Workflow`, and verified 96 tests, all generated page drift checks, desktop/mobile HTML screenshots, Reading shelf layout, and public-safety scan before the approved push.
- Replaced the Audio Library wallpaper with the project-owner supplied retail/listening-room interior image. Converted the JPEG to an EXIF-free 1440×806 RGB WebP at `assets/audio-room/owner-supplied-retail-listening-room.webp`, updated the Audio-scoped CSS and attribution report, preserved all 52 iPod cards and Liquid Glass panels, and verified 96 tests, all generated page drift checks, public-safety scan, plus desktop/mobile visual QA before the approved push.
- Published the owner-supplied App Library wallpaper in commits `c52ed64`, `742569b`, and a final mobile-overflow follow-up. Converted the PNG to an EXIF-free 1672×941 RGB WebP at `assets/app-room/owner-supplied-pantip-plaza-wallpaper.webp`, updated App-scoped wallpaper/glass CSS and public attribution, preserved all six diskette cards and functional LED signage, verified 97 tests plus all generated page drift checks, and production-read back matching App HTML/wallpaper assets. Live browser verification confirmed desktop rendering with no overflow and caught a mobile overflow from the LED/ambient layer that was fixed before final publication verification.
- Locally redesigned the App Library `เลือก App จากชั้นดิสก์` tools/search panel after owner feedback that its side edges looked clipped. Removed the tools panel from the generic header/footer glass grouping, rebuilt it as its own rounded glass retail panel with box-safe width, internal padding, balanced two-column alignment, subtle cyan/gold top light strip, and preserved the Windows 95 search module. Injected-preview browser checks at 1365px desktop and 390px mobile confirmed intact edges, no horizontal overflow, six App cards, correct shelf reconstruction, wallpaper loaded, and no broken images; publication remains pending a new explicit push.
- Locally added the owner-selected **Glass Circle** HTML download control to the Reading shelf. Each of the 27 generated book cards now renders an icon-only `<a download>` immediately after the publish date, with accessible Thai labels and no nested anchors; title and cover read links still open in new tabs. Regenerated `index.html` and verified 98 tests, all four generated-page checks, `git diff --check`, plus desktop and 390px browser preview with no overflow or broken images. Publication remains pending explicit Reading push approval.
## 2026-08-31

- Fixed the published Reading guide **Visual Art Director Agent — Interactive Reference Manual** side navigation so an opened mobile sidebar can be closed. Added an in-sidebar `×` close button visible at mobile widths, Escape close handling, synchronized `aria-expanded` states for the menu/collapse controls, and a direct `.is-open` sidebar class plus mobile-open CSS override strong enough to beat the closed transform. Regression coverage locks the close control. Local verification passed **99 tests**, all four generated-page drift checks, `git diff --check`, and a public-safety scan of the touched files before the approved scoped push.
- Added **Claude Interactive Manual** as the 28th Reading entry dated **6 July 2026** from `p2544/claude-interactive-course` commit `d4fdc70`. Built the Vite/React course into `claude-interactive-manual/`, kept runtime assets local and relative, retained the bundled `manual500p.pdf`, sanitized absolute home-directory example paths to `~/claude/`, and used the owner-selected Visual Art Director **Option 3 — Course Object** AI-generated cover normalized to `assets/covers/custom/claude-interactive-manual.webp` as an EXIF-free 600×900 RGB WebP. Local verification passed **103 tests**, all four generated-page drift checks, `git diff --check`, static desktop/mobile responsive checks, and a pre-share scan with no credential/private-path findings before the owner-approved push.
- Implemented the owner-selected **Option 4 — Pill Dock** shared footer locally across Reading, Audio, Gallery, and App generated pages. The footer uses the copy `Knowledge for Humans. Structured for AI.` and `READ · LISTEN · SEE · USE`, adds the owner-supplied Facebook icon as an icon-only link to the requested page without rendering the URL as visible text, and stores the stripped public WebP at `assets/icons/facebook-f.webp`. Regression coverage in `tests/test_shared_footer.py` verifies all four generated pages, the icon asset, no visible URL text, and mobile pill-dock CSS. Local verification passed **107 tests**, all four generated-page drift checks, and `git diff --check`; public push remains pending explicit approval.
- Reduced the shared **Option 4 — Pill Dock** footer scale by roughly 40% after owner feedback that the banner and `Knowledge for Humans. Structured for AI. READ · LISTEN · SEE · USE` typography were too large. The local CSS now uses a 540px pill width, proportionally smaller padding, icon hit area, and text sizes while keeping the Facebook URL hidden as icon-only link metadata.
## 2026-09-01

- Published the owner-supplied **Human–AI Communication Framework** as the 30th Reading entry in commit `3646f84`; the supplied bilingual HTML is preserved byte-for-byte at SHA-256 `95de03e50046b439a5a7deb2793359cf4da887001fe893d2e1be53fec5dc1fb9`, the cover is normalized to an EXIF-free 600×900 RGB WebP at SHA-256 `90ab0e70…`, public/private remote HEADs matched, Production returned 30 Reading cards with the new entry first, and live Production hashes matched Local for `index.html`, the manual, and the cover.
- Added the owner-supplied **GrokRouter — Interactive Reference Manual (TH)** as the 29th Reading entry dated **1 September 2026**. Replaced the initially staged HTML with the owner-supplied TH3 version, preserving its final standalone manual at SHA-256 `12ad8451…`; normalized the supplied cover only to an EXIF-free 600×900 RGB WebP at `assets/covers/custom/grokrouter-interactive-reference-manual.webp`. Regression coverage locks catalog metadata, 22 pages/navigation buttons, scroll-fix markers, JavaScript syntax, public-safety scan, and cover normalization. Local verification passed **110 tests**, all four generated-page drift checks, `git diff --check`, pre-share scan, and Playwright desktop/mobile checks for Reading shelf and manual with zero horizontal overflow; publication was explicitly approved in the current turn.
- Published the YouTube podcast **ระบบการสื่อสารระหว่างมนุษย์กับ Ai V1.0** (`AJ4cLr7A4Rc`) on channel **manny calavara** using the generated 36-scene 2D editorial storyboard video. The 22:15 H.264/AAC 1080p MP4 passed full decode and silence checks; YouTube Studio reported no copyright issues, Public visibility, Not made for kids, custom thumbnail, and exact `tech (Ai)` playlist membership. Added it as Audio Shelf position 1 with a 480×360 local cover; 53 audio entries now render from `data/audio-books.json`. Published in commit `681c1a9`; public and private remote HEADs matched, and production `audio-library.html` plus `assets/audio-covers/AJ4cLr7A4Rc.jpg` hash-matched Local with the new item first.

- Added and published-ready **Podcast Visual Storyboard & AI Image Prompting** as the 31st Reading entry from the owner-provided public Markdown source. Built it into the established 10-view Interactive Reference Manual pattern with sidebar/topbar navigation, hash routing, search, theme/font controls, copy buttons, print CSS, and no reader-facing `ค่ะ`. Used the owner-supplied final cover as the active shelf cover, preserving visible design by resizing/padding only to an EXIF-free 600×900 RGB WebP at `assets/covers/custom/podcast-visual-storyboard-ai-prompting.webp`. Local verification passed **118 tests**, all four generated-page drift checks, `git diff --check`, and a public-safety scan; the owner approved push in the current turn.

## 2026-09-02

- Produced the owner-approved podcast visual storyboard video **เมื่อ AI ทำงานแทนจนเงินไร้ค่า: Demand Collapse และโลกหลังทุนนิยม** from the supplied Drive audio. The workflow created a 25-scene hand-drawn editorial business/storyboard sequence, rendered an 11:40 H.264/AAC 1080p MP4 with a custom Thai thumbnail, archived the storyboard package and final MP4 back to the private project workspace, and published the video on YouTube as Unlisted video ID `sKC0mlraNPo` on channel **manny calavara**. YouTube oEmbed/public watch read-back returned the exact title, author, and thumbnail; after owner follow-up, added it locally as Audio Shelf position 1 with `assets/audio-covers/sKC0mlraNPo.jpg`, shifting the prior 53 entries to positions 2–54.
- Replaced the published Reading guide **Podcast Visual Storyboard & AI Image Prompting** with the owner-supplied final HTML file, preserving the replacement byte-for-byte at SHA-256 `145ffc4cf7d08a74b78ebb6ab8d11b41931a00533b49d04699c6ad5f066fc4fc`. Regression coverage now locks the 23-view/23-nav Interactive Reference Manual v2 shape, JavaScript syntax, public-safety scan, and continued catalog/cover/source checks; the owner explicitly approved immediate push in the current turn.
## 2026-09-03

- Locally added square icon-only HTML download controls to every App Shelf card, positioned immediately after each publish date and pointing at the same single HTML App launcher/source with a real `download` attribute. Split the formerly whole-card App link into separate title and diskette open links to avoid nested anchors, regenerated `app-library.html`, and added regression coverage for six controls, href parity, after-date placement, no nested anchors, and CSS glass-square markers. Focused App tests, App generator check, scoped diff check, pre-share scan, and Playwright desktop/mobile previews passed; full project tests are blocked by an unrelated pre-existing Audio test/catalog URL mismatch for `sKC0mlraNPo`. Publication remains pending explicit App push approval.
- Published the podcast **รันธุรกิจอัตโนมัติด้วย Hermes Agent: จาก Chatbot สู่ระบบธุรกิจที่ทำงานเอง** (`6EOEMjBM6HU`) on YouTube channel **manny calavara** as Public with Not made for kids, clean Studio checks, and exact `tech (Ai)` playlist membership verified on the public playlist page. The approved 34-scene hybrid hand-drawn editorial + architectural cutaway storyboard rendered to a 23:42 H.264/AAC 1080p MP4; final video QA passed decode, duration delta 0.004s, no silence events, and thumbnail/frame visual checks. Added the item locally as Audio Shelf position 1 with a 480×360 cover, shifted prior positions, regenerated `audio-library.html`, fixed the Audio ambient-light mobile overflow, and verified 121 tests plus all generated-page drift checks; public Library push remains pending explicit approval.

## 2026-09-05

- Published the podcast visual storyboard **เมื่อ AI โคลนตัวตนเรามาสวมรอย: บอทสแกมและโลกอินเทอร์เน็ตหลังความจริง** (`wTrLXC427hg`) on YouTube channel **manny calavara** as Public, Not made for kids, with Studio checks reporting no issues and public oEmbed returning the exact title/author/thumbnail. The NotebookLM/Gemini Thai Deep Dive was rendered as a 34-scene 08+12 editorial collage + blueprint systems visualization video; final MP4 QA verified H.264 1920×1080 yuv420p, AAC 48 kHz stereo, 23:34 duration, readable Thai overlay frames, and public watch/thumbnail HTTP 200. Added it as Audio Shelf position 1 with a 480×360 cover, shifted prior positions, regenerated `audio-library.html`, and verified 122 tests plus all generated-page drift checks. Published the Library update in commit `4dd48bb`; public/private remote HEADs matched and Production returned 56 audio cards with `wTrLXC427hg` first/newest and the live cover hash matching Local.
- After owner feedback that the edited clip must not include a bottom title frame or image-detail text, rendered and uploaded a clean replacement video with no editing overlay/lower-third band. Published the clean version as Public video `WQMJBmgPSi8`, verified Studio checks reported no issues and public oEmbed returned the exact title/author/thumbnail, then replaced the Audio Shelf entry so the collection still has 56 cards and the first item points to the clean upload with `assets/audio-covers/WQMJBmgPSi8.jpg`. Local desktop/mobile previews, 122 tests, all generated-page drift checks, and `git diff --check` passed before the scoped replacement push.

## 2026-09-07

- Added and published the owner-supplied **Hermes Bot Mode · Interactive Reference Manual** as the 32nd Reading entry in commit `956de3d`. The supplied HTML is preserved byte-for-byte at SHA-256 `c7724865fe16653fe4156810138693e4e836ece4d45636be38e1f8d50db12258`; the owner-supplied cover was normalized only to an EXIF-free 600×900 RGB WebP at `assets/covers/custom/hermes-bot-mode-interactive-manual.webp` with SHA-256 `f996efbc…`. Full verification passed 125 tests, all four generated-page drift checks, `git diff --check`, staged pre-share scan, local Playwright desktop/mobile preview, remote HEAD equality for public/private remotes, production HTTP hash read-back for index/manual/cover, and production Playwright shelf/manual checks with zero horizontal overflow.
- Published **HEIC to JPG Batch Converter** from `starlink2569/heic2jpg` commit `ee777bc` as the seventh App Shelf entry in commit `ea872a7`. Imported the browser runtime under `app/heic2jpg/` as a hardened derivative, removed external Google Fonts requests, added an 80 MB per-file guard, kept local `heic2any`/`JSZip` vendor files, and excluded the HEIC sample, Node server, package lock, and Git metadata. Regression locks the pinned upstream `index.html` SHA-256 `15a05341…`, stable wrapper `app/heic2jpg.html`, sandbox/referrer policy, missing upstream LICENSE note, and local vendor dependency metadata. Verification passed 126 tests, all generated-page drift checks, `git diff --check`, staged pre-share scan, HTTP hash read-back, and production Playwright desktop/mobile smoke conversion using the upstream sample HEIC with zero overflow; public and private remote HEADs matched.
- Locally updated **HEIC to JPG Batch Converter** to upstream commit `05b4279` after the source repo added an app icon and interface branding. Kept the prior Library hardening (no external Google Fonts and 80 MB per-file guard), imported `assets/app-icon.png` unchanged as a 1254×1254 RGB PNG, updated catalog provenance/date/summary, regenerated `app-library.html`, and verified focused App tests, 126 full tests, all generated-page drift checks, `git diff --check`, pre-share scan, and desktop/mobile Playwright conversion smoke test locally. Publication remains pending explicit push approval for this update.
- Added and publication-verified locally the owner-supplied **17 ขั้นตอนการใช้ Grok Bot อย่างมืออาชีพ — Interactive Reference Manual** as the 33rd Reading entry. The supplied bilingual Grok Bot manual content was copied to `grok-bot-interactive-manual.html`; a minimal technical `html{overflow-x:hidden}` containment was added after browser QA found 60px mobile horizontal overflow from the off-canvas sidebar, without changing the manual prose or visible cover design. The final HTML SHA-256 is `9500ea5cbb70cf89108c6f97f5193907fe108eb017c2a386b4bed0bd4257f669`. The owner-supplied cover was normalized only to an EXIF-free 600×900 RGB WebP at `assets/covers/custom/grok-bot-interactive-manual.webp` with SHA-256 `87a107f5…`. Local verification passed 129 tests, all four generated-page drift checks, `git diff --check`, a pre-share scan, and Playwright desktop/mobile checks for the Reading shelf and manual with zero horizontal overflow; the owner explicitly approved push in the current turn.
- Published **17 ขั้นตอนการใช้ Grok Bot อย่างมืออาชีพ — Interactive Reference Manual** in commit `fea7cb5`; public and private remote HEADs matched `fea7cb5252c5f47187635be2e5051592875df2dd`. Production HTTP read-back hash-matched Local for `index.html` (`be4863c5…`), `grok-bot-interactive-manual.html` (`9500ea5c…`), and the cover WebP (`87a107f5…`). Production Playwright checks confirmed 33 Reading cards with the new guide first, 33 manual pages/nav buttons, working language toggle/search, 600×900 live cover, and zero horizontal overflow on desktop and 390px mobile. GitHub CLI Actions metadata was unavailable because `gh` was not authenticated, so deployment was verified by remote HEAD equality plus production read-back.
- Locally added **Travis Pocket — ฝึกกีตาร์ออฟไลน์** from `p2544/travis-picking` commit `093c364` as the eighth App Shelf entry. Preserved the upstream single-file `docs/index.html` byte-for-byte at `app/travis-pocket.html` with SHA-256 `f7e69325cdbeff6f0a64c50a237457f673f0b47bc66e505272ff5e548ef06701`, catalogued it under `Music Practice` with a text-first TP diskette label, regenerated `app-library.html`, and verified 130 tests plus all generated-page drift checks, `git diff --check`, HTTP read-back, static DOM/card/link checks, and public-safety scan locally. Desktop/mobile browser visual QA is still pending because local browser navigation and temporary Playwright execution were blocked in this environment; public push remains pending explicit approval.
- Published **Travis Pocket — ฝึกกีตาร์ออฟไลน์** in commit `82c4f91`; public and private remote HEADs matched `82c4f9182b01008cde5f0f5d763b836617e1e92d`. Production HTTP read-back hash-matched Local for `app-library.html` (`07cb6619…`) and `app/travis-pocket.html` (`f7e69325…`), and the public App Shelf now has 8 App cards with Travis Pocket first/newest. GitHub CLI Actions metadata and browser visual QA were unavailable in this environment, so deployment was verified by remote HEAD equality plus production read-back.

- Produced and published the NotebookLM podcast visual storyboard **สับสวิตช์ดับทุกข์ด้วยฟิสิกส์ควอนตัม: ไตรลักษณ์ใน Quantum Physics** (`IrGtonl70Ng`) from the Gemini Notebook `The Three Marks of Existence in Quantum Physics`. The workflow extracted and validated the 20:14 M4A audio, created a 24-scene no-text/no-frame visual storyboard using owner-selected Styles 12 + 21 + 10, rendered a clean H.264/AAC 1080p MP4, archived the audio/transcript/storyboard/final video package to the private project Drive folder, and published the video Public on YouTube. YouTube Studio read-back reported checks complete/no issues, Public visibility, and the public watch page returned the exact title. Added the video locally as the newest Audio Shelf entry with `assets/audio-covers/IrGtonl70Ng.jpg`, regenerated `audio-library.html`, and updated Audio regression coverage for 57 items. Final Library verification/push is covered by the current session gates.
## 2026-09-09

- Published the owner-supplied Reading guide **งานของผม** in commit `31c6756`. The supplied HTML was copied byte-for-byte to `WHAT-I-DO-final.html` at SHA-256 `d81ba5282a99e4089e57721f0e3e5b5524d653cf05ba1ec4da4644bbe5389051`; the owner-supplied PNG cover was normalized only to an EXIF-free 600×900 RGB WebP at `assets/covers/custom/what-i-do.webp` with SHA-256 `5fbff9c45c3cb861752fada07d0765bf479bf28288ab4b93b6a89c2b33e34c4c`. Local verification passed 133 tests, all four generated-page drift checks, `git diff --check`, pre-share scan, and Playwright desktop/mobile checks. Public/private remote HEADs matched `31c6756317e8bed0b1310b9348ec29cf8fd4e1d7`; Production HTTP read-back hash-matched Local for `index.html`, `WHAT-I-DO-final.html`, and the cover, and Production Playwright confirmed 34 Reading cards with the new guide first, search returning one result, 21 page sections, 20 nav items, 600×900 live cover, and zero horizontal overflow. GitHub CLI Actions metadata was unavailable because `gh` was not authenticated, so deployment was verified by remote HEAD equality plus production hash/DOM read-back.
- Corrected the **งานของผม** Reading cover in commit `a716576` after owner feedback that the previous contain/padded normalization left white margins and did not look like a real shelf book. Rebuilt `assets/covers/custom/what-i-do.webp` as an EXIF-free 600×900 RGB WebP using full-height scale plus narrow center crop, with no added white padding/border and no text/color/design edits. Full local gates still passed 133 tests and all generated-page checks; public/private remote HEADs matched `a71657666edeafaf0006d498bca2645168202d8a`; Production cover hash matched Local at `ef849089b23ef5e13c0065b8bfce8ce5aa6b597e9e44eb481a57595ab4075bba`; Production Playwright desktop/mobile confirmed 34 Reading cards, the guide first, 600×900 live cover, and zero horizontal overflow.
- Replaced the published **งานของผม** Reading guide and cover with the owner-supplied mobile-fixed HTML and final physical-book cover in commit `d260006`. The stable public URL and catalog record were preserved; `WHAT-I-DO-final.html` now matches the supplied replacement SHA-256 `98d44ad1f69aede0b8015cd343984485461f2b67971338e774e0c02eddef3e0a`, and the cover is an EXIF-free 600×900 RGB WebP at SHA-256 `113629318f32a35fd328b2090388fc0270cf50c54a5fd73470cea3845916d549`. Local gates passed 133 tests, all four generated-page checks, `git diff --check`, pre-share scan, and Playwright desktop/mobile preview; public/private remote HEADs matched `d260006ce549e80c6d562bed8e12858502de368a`, and Production hash/DOM read-back confirmed the replacement HTML, cover, 34-card Reading shelf, 21 sections, 20 nav items, and zero horizontal overflow.
- Added the CSS 3D front-cover peek effect to every Reading Shelf book in commit `1bb731e`. Generated book cards now separate the stationary paper block (`book-cover-volume::before`) from the real cover image, remove the old messy cover backing, keep the hover target/card still, and rotate only the cover from the left spine by `rotateY(-24deg)` with `perspective: 1100px`, `1.05s cubic-bezier(.42, 0, .2, 1)` motion, keyboard `:focus-visible`, and `prefers-reduced-motion` support. Local verification passed 134 tests, all four generated-page checks, `git diff --check`, pre-share scan, and desktop/mobile Playwright checks; public/private remote HEADs matched `1bb731e6d61aecf8318de8f2aa02b98677877cb0`, Production `index.html` and `assets/css/reading-library.css` hashes matched Local, and live Production confirmed 34 Reading cards/volumes/covers, transparent cover backing, `1100px` perspective, front-cover matrix3d hover rotation, stationary card transform, reduced-motion transform suppression, and zero horizontal overflow.
- Lowered every Reading Shelf book closer to the shelf in commit `d65bd70` after owner review of the CSS 3D cover effect. The Reading CSS now keeps the 3D cover wrapper padding at `16px 16px 0` and sets the Reading `book-grid` bottom padding to `.875rem`, producing a measured 3px gap between cover bottoms and the shelf plank on both desktop and 390px mobile while preserving the front-cover hinge animation, stationary card transform, 34-card count, and zero horizontal overflow. Local verification passed 134 tests, all four generated-page checks, `git diff --check`, pre-share scan, and local Playwright geometry checks; public/private remote HEADs matched `d65bd703c044ab939356823f77e2fb7382a40bd6`, Production CSS/test hashes matched Local, and Production Playwright confirmed 34 Reading cards/volumes with `firstGapPx`, `minGapPx`, and `maxGapPx` all equal to 3px.
- Fixed the Reading Shelf CSS 3D hover visibility in commit `4a50a41` after owner reported that hovering over books did not visibly reveal the cover. Production reproduction showed hover was firing, but the strongest `perspective: 1100px` context was on the outer wrapper rather than the direct `book-cover-volume` parent, making the `rotateY(-24deg)` cover movement appear too flat; the fix adds `perspective: 1100px` to `book-cover-volume` and also triggers the same cover-only rotation from `.book-card:hover` while keeping `cardTransform: none`, the 3px shelf gap, keyboard focus, and reduced-motion suppression. Local verification passed 134 tests, all four generated-page checks, `git diff --check`, pre-share scan, and before/after Playwright visual checks with substantial pixel change; public/private remote HEADs matched `4a50a418ada63a54df2c6b848f4773b98a861046`, Production CSS/test hashes matched Local after cache propagation, and Production Playwright confirmed 34 cards/volumes, visible matrix3d cover rotation on desktop/mobile, 3px gaps, no card movement, reduced-motion suppression, and zero horizontal overflow.
- Published the real Reading Shelf hover fallback in commit `4ea0432` after the owner confirmed the standalone v2 JavaScript fallback test worked and explicitly approved push. The Reading CSS now opens the cover from `.book-card:hover`, `.book-card.is-open`, and `.book-cover-link:hover` without a hover media-query gate, while `assets/js/library.js` adds Reading-only pointer/mouse/focus fallback events that toggle `.is-open` on the stable card hit area and clear it when cards are hidden. The Reading template cache-busts both `reading-library.css` and `library.js` with `v=reading-cover-hover-v3`. TDD RED/GREEN checks were run for the fallback and cache-bust rules; full local gates passed 134 tests, all generated-page checks, `git diff --check`, and a pre-share scan. Public/private remote HEADs matched `4ea0432f433ca5b2a51280b1c1f27369e0874aa1`; Production HTTP hash read-back matched Local for `index.html`, `assets/css/reading-library.css`, and `assets/js/library.js` after propagation; Production Playwright verified desktop/mobile real-shelf hover with 34 cards, v3 assets loaded, `.is-open=true`, cover `matrix3d(...)`, `cardTransform: none`, `perspective: 1100px`, zero overflow, and no console/page errors.
- Locally replaced the **งานของผม** / What I Do Reading cover with the owner-supplied new physical-book PNG cover. The source PNG SHA-256 is `24c7c603393310345dfe440ad6799f66e8049408c3759e25fbcd4dc037ef205d`; the public cover derivative is `assets/covers/custom/what-i-do.webp`, an EXIF-free 600×900 RGB WebP at SHA-256 `ff4e8f4b20121f10c46a88fc263581411af431e476fb3fc44bcb3869b7b6a1fc`, created by resizing 915×1377 to 600×903 and center-cropping only the narrow vertical excess with no padding, no added border, and no text/color/design edits. Focused RED/GREEN cover regression, full 134-test suite, all generated-page checks, `git diff --check`, pre-share scan, and local desktop/mobile Playwright shelf checks passed; publication remains pending explicit push approval.
- Produced and published the no-text podcast visual storyboard **งานของผม** (`LmGIx3NT4tQ`) from the owner-supplied Drive audio. The 33:45 H.264/AAC 1080p MP4 uses Style 27 as the main quiet-luxury business-documentary direction with Style 20 architectural cutaway and Style 12 blueprint systems scenes inserted for system moments; it intentionally contains no captions, subtitle overlays, lower thirds, title cards, frames, labels, logos, or watermark text. YouTube Studio read-back confirmed Public visibility, Not made for kids, custom no-text thumbnail, Copyright checks complete/no issues, and exact `tech (Ai)` playlist selection; public watch-page extraction returned the exact title and playlist membership. Added it as the newest Audio Shelf entry with `assets/audio-covers/LmGIx3NT4tQ.jpg`, regenerated `audio-library.html`, and verified 135 tests plus all generated-page drift checks, `git diff --check`, local desktop/mobile Audio Shelf previews, and a public-safe scan of touched Audio files.

## 2026-09-10

- Locally added **ธรรมะจากท่านพุทธทาส — Buddhadasa Audio Archive** as the ninth App Shelf entry from `starlink2569/starlink2569.github.io` commit `0ee4efbdf8f9137b24e60cd3a86cfa0c46f57f0e` under the stable launcher `app/buddhadasa-audio.html`. Imported the static audio archive runtime under `app/buddhadasa-audio/`, recorded the upstream `index.html` SHA-256 `f90e3036…`, stripped public runtime image metadata, removed the Google Fonts dependency, added URL/string hardening around OneDrive track links, and used the owner-supplied Buddhadasa portrait as the EXIF-free diskette sticker `assets/app-stickers/buddhadasa-audio.webp`. Local verification passed 136 tests, all four generated-page drift checks, `git diff --check`, App Shelf/runtime Playwright desktop/mobile checks, JavaScript/JSON syntax checks, and a public-safety scan; public push remains pending explicit App push approval.
- Published **ธรรมะจากท่านพุทธทาส — Buddhadasa Audio Archive** in commit `2e272ad`; public and private remote HEADs matched `2e272ad5c17ee3eb29c3d58289c381277dd5c878`. Production HTTP hash read-back matched Local for `app-library.html`, the launcher, runtime HTML, runtime JS, `audio-index.json`, and the owner-supplied sticker WebP. Production Playwright desktop/mobile checks confirmed 9 App cards with Buddhadasa Audio first/newest, the 420×361 sticker loaded, runtime redirect worked, 1,493 tracks rendered with 80 initial visible results, OneDrive HTTPS links were present, and zero horizontal overflow. GitHub CLI Actions metadata was unavailable because `gh` was not authenticated, so deployment was verified by remote HEAD equality plus production hash/DOM read-back.
- Locally adapted the Reading Shelf hover implementation to match the owner-supplied `reading-cover-hover-test-v2.html` behavior more closely after the live shelf still did not show the desired effect for the owner. The real shelf now uses the v2-style stronger `rotateY(-42deg)` reveal, `.42s cubic-bezier(.2,.8,.2,1)` transition, absolute cover layer, visible page-block/spine layer, CSS hover without a media gate, `.is-open` fallback, and document-level `mousemove`/`pointermove`/`mouseover`/`pointerover` capture using `event.target.closest(".book-card")` plus `elementFromPoint`. The Reading template cache-busts both CSS and JS with `v=reading-cover-hover-v5`. TDD RED/GREEN covered the missing v2 markers, local browser checks confirmed both CSS hover and JS fallback open to `matrix3d(0.743145...)` while keeping `cardTransform: none`, and full local gates passed 136 tests plus all generated-page checks. Published in commit `7354479`; public/private remote HEADs matched `73544795e8b25acb2d34aac2ebcf67656c6c8327`, Production HTTP hash read-back matched Local for `index.html`, `assets/css/reading-library.css`, `assets/js/library.js`, and `docs/wiki/log.md`, and Production browser checks confirmed CSS/JS v5, CSS hover and JS fallback both opening to `matrix3d(0.743145...)`, `cardTransform: none`, `perspective: 1100px`, `.42s` transition, 34 Reading cards, and zero overflow.
- Locally restored the Reading Shelf cover-peek implementation to the owner-supplied `message.txt` CSS 3D contract after feedback that the v5/v6 attempts were wrong. The prepared v7 change removes the `.is-open` JavaScript fallback entirely, returns hover behavior to CSS only under `@media (hover: hover)`, rotates only `.book-cover` from the left spine by `rotateY(-24deg)`, restores the `1.05s cubic-bezier(.42, 0, .2, 1)` timing, keeps rectangular book corners (`1px 3px 3px 1px`) and the original thin stationary paper block (`z-index: -1`, `inset: 2px -7px 0 3px`), preserves `perspective: 1100px`, keyboard focus, reduced-motion suppression, 3px shelf clearance, and Reading-scoped selectors only. The Reading template cache-busts CSS/JS with `v=reading-cover-hover-v7`. TDD RED/GREEN, full 136-test gates, generated-page checks, `git diff --check`, pre-share scan, and local desktop/mobile browser checks passed; publication remains pending explicit push approval.
- Locally squared the Reading Shelf book object after the owner confirmed the hover effect works but requested no rounded book corners. The prepared v8 change keeps the working CSS-only `rotateY(-24deg)` front-cover peek, stationary paper block, 3px shelf clearance, keyboard focus, and reduced-motion behavior, but sets the visible cover, paper-block pseudo-element, and cover-link focus outline radii to `0`; Reading assets are cache-busted with `v=reading-cover-hover-v8`. Focused RED/GREEN, full 136-test gates, all four generated-page drift checks, `git diff --check`, pre-share scan, and local Playwright desktop/mobile browser checks passed; browser verification confirmed 34 Reading cards, `coverRadius/paperRadius/focusRadius` all `0px`, hover `matrix3d(0.913545...)`, `cardTransform: none`, `perspective: 1100px`, 600×900 cover dimensions, zero overflow, and no console/page errors. Publication remains pending explicit push approval.
- Published the squared Reading Shelf book-object correction in commit `5f6d761` after the owner explicitly approved `push`. Public and private remotes matched `5f6d761e57fa874c7c1cf9f586650f80023767f1`; Production HTTP hash read-back matched Local for `index.html`, `assets/css/reading-library.css`, and `assets/js/library.js` after propagation. Production Playwright desktop/mobile checks confirmed 34 Reading cards with `งานของผม` first, CSS/JS `reading-cover-hover-v8`, cover/paper/focus radii all `0px`, hover `matrix3d(0.913545...)`, `cardTransform: none`, `perspective: 1100px`, 600×900 cover dimensions, zero overflow, and no console/page errors.

- Published the **Hermes Bot Mode · Interactive Reference Manual** Reading cover replacement in commit `42f2a61` after the owner explicitly approved `push`. The owner-supplied PNG source SHA-256 is `4a18827d9bc397ad32ea09857f3b6c4bc7d935e5984f7e87416b9d1b753b231b`; the public derivative is `assets/covers/custom/hermes-bot-mode-interactive-manual.webp`, an EXIF-free 600×900 RGB WebP at SHA-256 `f92982e0a4e9f003a2a4a40b765b3b8a62a0ea22e9411ec7b5d6b5f5a86f0064`, created by resizing 1003×1455 to 620×900 and center-cropping x=10..610 with no padding, no added white border, and no text/color/design edits. Full local gates passed 136 tests plus all generated-page checks and `git diff --check`; public/private remote HEADs matched `42f2a610e336bc4833b2de3d3261eb410aa83ad3`; Production HTTP read-back matched Local for `index.html` and the cover after propagation, and Production Playwright desktop/mobile checks confirmed 34 Reading cards, Hermes Bot Mode card index 2, 600×900 live cover, zero overflow, and no console/page errors.

## 2026-09-13

- Published the owner-supplied **One Project. Any AI. — คู่มือใช้ Codex และ Claude ร่วมกัน** as the 35th Reading entry in commit `0ab1b13` after the owner explicitly approved `commit push`. The supplied HTML is preserved byte-for-byte at `codex-claude-shared-project-folder-manual-th-v4-complete.html` with SHA-256 `39bac142899520970330ce1909bf0f0b09e18f9c7fb59ccb3d3f859a98ac04e4`; the owner-supplied cover was normalized only to an EXIF-free 600×900 RGB WebP at `assets/covers/custom/codex-claude-shared-project-folder-manual.webp` with SHA-256 `cbed26a1c2af452ee58e9127b57e957c57eb265542729e8747a6645157f7cea6`. The same commit fixed the Reading Shelf hover by cache-busting CSS/JS to `reading-cover-hover-v9`, keeping the owner-approved CSS-only `rotateY(-24deg)` contract, adding `@media (hover: hover), (any-hover: hover)`, and restoring `.book-card:hover .book-cover` as a stable trigger while keeping `cardTransform: none`, `perspective: 1100px`, square corners, keyboard focus, and reduced-motion suppression. Full pre-push gates passed 140 tests plus all generated-page drift checks, `git diff --check`, and a pre-share scan. Public/private remotes matched `0ab1b13dc85d00a76edd414ee7b5faf7e7d99e5b`; Production HTTP hash read-back matched Local for index, manual, cover, Reading CSS, and shared JS. Production Playwright desktop/mobile checks confirmed 35 Reading cards with the new guide first, v9 CSS/JS loaded, desktop hover opening to `matrix3d(0.913545...)`, `cardTransform: none`, `perspective: 1100px`, 600×900 live cover, 22 manual sections/nav buttons, zero horizontal overflow, and no console/page errors.
- Published a stronger Reading Shelf cover-peek v10 in commit `6d72574` after the owner reported that the live shelf still did not visibly “เผยอปก” and then approved `push v10`. Production reproduction showed v9 was technically active (`matrix3d(0.913545...)`, CSS/JS v9, hover media true) but visually subtle: the shelf-thumbnail cover mostly looked flat. The v10 change promotes the visible angle to `rotateY(-42deg)`, shortens timing to `.42s cubic-bezier(.2,.8,.2,1)`, makes the cover an absolute full front layer with `object-fit: cover`, brings the stationary paper block to `z-index: 0`, adds `.book-cover-link:hover`, and cache-busts CSS/JS to `reading-cover-hover-v10`. Full local gates passed 140 tests plus all generated-page drift checks, `git diff --check`, and a pre-share scan. An initial Pages deployment at `6d72574` failed at the deploy step, so an empty retry commit `83ab9f8` was pushed; the retry Pages run completed successfully. Public/private remotes matched `83ab9f83833e1bf199a4344e70280730c6441282`, Production HTTP hash read-back matched Local for `index.html`, `assets/css/reading-library.css`, `assets/js/library.js`, and this log, and Production Playwright desktop/mobile checks confirmed 35 cards, first title `One Project. Any AI.`, CSS/JS v10, desktop hover `matrix3d(0.743145...)`, `cardTransform: none`, `perspective: 1100px`, absolute cover layer, paper `z-index: 0`, 600×900 cover, zero overflow, and no console/page errors.
- Published the v11 Reading Shelf correction in commit `f89eaf0` after the owner explicitly said `push v11`. This restores the owner-supplied `CSS_3D.txt` contract after v10 drifted into a stronger visibility experiment (`-42deg`, absolute cover layer, `z-index: 0`, faster timing). v11 locks the contract with regression tests: separate cover/paper layers, stable non-moving hover area, `rotateY(-24deg)`, `perspective: 1100px`, `1.05s cubic-bezier(.42, 0, .2, 1)`, stationary page block at `z-index: -1`, cover as a relative real image layer with `height: auto`, sample corner radii `1px 6px 6px 1px` / `1px 3px 3px 1px`, keyboard focus, reduced-motion suppression, no `.is-open` fallback, no broad `.book-card:hover` trigger, no click/PDF/page-turn behavior, and cache-bust `reading-cover-hover-v11`. TDD RED confirmed the v10 drift before implementation; pre-push gates passed 140 tests plus all generated-page drift checks, `git diff --check`, and pre-share scan. Public/private remotes matched `f89eaf0bd6745c35739711d5b7f1ce393ac87d19`. Production HTTP hash read-back matched Local for `index.html`, `assets/css/reading-library.css`, and `assets/js/library.js`. Production Playwright QA confirmed 35 cards, first title `One Project. Any AI.`, CSS/JS v11, hover `matrix3d(0.913545...)`, transition `1.05s`, `cardTransform: none`, `wrapPerspective/volPerspective: 1100px`, `coverPosition: relative`, paper `z-index: -1`, cover/paper radii matching the supplied sample, reduced-motion transform `none`, zero overflow, and no console/page errors.
- Locally prepared a v12 hybrid-mouse hover fix after the owner reported that v11 still did not open on mouse hover. Production diagnosis reproduced why this can happen on hybrid/touch-capable environments: with `hasTouch` emulation the live v11 page loaded `reading-cover-hover-v11`, the card/link/wrap all matched `:hover`, but `matchMedia('(hover: hover)')` and `matchMedia('(any-hover: hover)')` were false and the CSS-only media-gated rule did not apply, leaving the cover at identity transform. The prepared v12 keeps the `CSS_3D.txt` motion/layer contract unchanged (`rotateY(-24deg)`, `1.05s cubic-bezier(.42, 0, .2, 1)`, relative cover layer, paper `z-index: -1`) but restores robust hybrid support: CSS uses `@media (hover: hover), (any-hover: hover)`, the Reading-only JS adds/removes `.is-hovering` on stable `.book-card` via `pointerenter`/`mouseenter` and capture-phase `mousemove`/`elementFromPoint`, hidden cards clear the state, reduced-motion still suppresses transform, and CSS/JS are cache-busted to `reading-cover-hover-v12`. TDD RED confirmed v11 failed the new hybrid regression, then focused tests, 140-test full suite, generated-page drift checks, `git diff --check`, and pre-share scan passed. Local browser QA confirmed v12 opens on both normal desktop and hybrid/touch-emulated mouse hover even when media hover is false, while keeping `cardTransform: none`, `wrapPerspective/volPerspective: 1100px`, `coverPosition: relative`, paper `z-index: -1`, zero overflow, and no console/page errors. Publication remains pending explicit push approval.
