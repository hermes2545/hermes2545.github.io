FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PORT=8080

WORKDIR /app
COPY requirements-cms.txt ./
RUN pip install --no-cache-dir -r requirements-cms.txt
COPY . ./

CMD ["python", "-m", "cms_backend.server"]
